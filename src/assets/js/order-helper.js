function calculateOrder(divId, tableId) {
	console.log(`calculateOrder calisti divId:${divId} | tableId:${tableId}`)
	notifyMe(`calculateOrder calisti divId:${divId} | tableId:${tableId}`,'blue')
}


