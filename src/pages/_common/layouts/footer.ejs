<!-- <footer class="footer mt-5 mt-md-1"> -->
<footer class="d-flex justify-content-between align-items-center">
	<div class="d-flex flex-md-fill justify-content-md-start align-items-center">
		<div class="ms-2 d-flex">
			<a href="javascript:modalFormOptions()" class="btn btn-info px-2 " title="Form Ayarları"><i class="fas fa-cogs"></i></a>
			<% if(config.status!='release'){ %>
			<a href="javascript:reloadHaham()" class="btn btn-success ms-2 px-2" title="Reload Haham Engine"><i class="fas fa-sync"></i></a>
			<% } %>
		</div>
		<div class="mt-1 mx-3">&copy;<%- (new Date()).getFullYear() %> Ritalin <span id="innerWidth" class="ms-2"></span></div>
	</div>
	<div class="d-md-flex ms-2 justify-content-end text-end align-items-center">
		<% if(config.status!='release'){ %>
		<div class="mx-3" title="status">
			status:
			<%- config.status || '' %>
		</div>
		<% } %>
		<div class="mx-3" title="Aktif kullanıcı">
			<i class="fas fa-user me-1"></i>
			<%- session.username %>
		</div>
		<div id="dbLabel" class="mx-3" title="Aktif veri ambarı">
			<i class="fas fa-database me-1" title="dbId: <%- dbId %>"></i>
			<%- session.dbName %>
		</div>
	</div>
</footer>
<script type="text/javascript">
document.querySelector('#innerWidth').innerHTML = `${window.innerWidth}x${window.innerHeight} px`
</script>